list_of_tuples = [(1, 2), (2, 3), (3, 4)]

# Convert each tuple to a list and store them in a new list
list_of_lists = [list(tup) for tup in list_of_tuples]

print("Original list of tuples:", list_of_tuples)
print("Converted list of lists:", list_of_lists)
